/* Public-safe view: preserve all mapping logic without publishing live records. */
window.renderPublicDictionary=function(dashboard,dictionary,registry,domain){
  const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const title=domain[0].toUpperCase()+domain.slice(1),metrics=registry.metrics;
  const views={inbound:['inbound-call-dashboard','northstar-call-center'],outbound:['outbound-call-dashboard','northstar-outbound-calls'],agent:['agent-performance-dashboard','agent-data']}[domain];
  const groups=['All metrics',...new Set(metrics.map(m=>m.group))];let group='All metrics',search='';
  const notice='<div class="io-scope-warning"><b>Public reference version · live data withheld.</b> No agent names, user IDs, performance records, raw API responses or credentials are included. Definitions and query templates are retained. Empty data is not zero and no public result is marked API- or Assistant-verified.</div>';
  const switchView=view=>document.querySelector(`[data-call-center-tab="${view}"]`)?.click();
  dashboard.innerHTML=`<main class="io ag-workspace"><header class="io-head"><div><span class="io-eyebrow">Call center operations / public reference</span><h1>${title} ${domain==='agent'?'performance':'dashboard'}</h1><p>The dashboard implementation is preserved. Operational charts require a private, authorized data connection.</p></div><button class="io-btn primary" data-public-dictionary>Open ${domain} data dictionary ↗</button></header>${notice}<section class="io-card"><h2>Mapping ready for private implementation</h2><p>Explore ${metrics.length} metric definitions below. Query filters, formulas, reporting grains, API endpoints and warehouse mappings are preserved; no fake records replace private data.</p><div class="ag-links">${groups.slice(1).map(g=>`<button data-public-topic="${esc(g)}">${esc(g)} ↗</button>`).join('')}</div><p><a href="PUBLICATION.md">Private setup and publication policy</a></p></section></main>`;
  dictionary.innerHTML=`<main class="io ag-workspace"><header class="io-head"><div><span class="io-eyebrow">Call center operations / public reference</span><h1>${title} data dictionary</h1><p>Amazon source fields and reusable semantic mappings, without production records.</p></div><div class="io-actions"><a class="io-btn" href="data/${domain}-operations-registry.json" download>Download mapping ↓</a><button class="io-btn primary" data-public-dashboard>Back to dashboard ↗</button></div></header>${notice}<div class="io-dictionary-layout"><aside class="io-dictionary-nav" aria-label="Metric topics">${groups.map(g=>`<button data-public-group="${esc(g)}">${esc(g)}</button>`).join('')}</aside><section><div class="io-search"><input data-public-search type="search" aria-label="Search metric mappings" placeholder="Search metric, Amazon field or definition…"><span data-public-count aria-live="polite"></span></div><div data-public-definitions></div><details class="io-card io-contract"><summary>Shared Snowflake / semantic contract</summary><pre>${esc(JSON.stringify(registry.warehouse,null,2))}</pre><p>Provision private ingestion, roles and server-side AWS configuration before loading production values. No browser credentials. Preserve raw evidence privately and do not commit it.</p></details>${domain==='agent'?'<details class="io-card io-contract"><summary>Agent identity and current-state mapping</summary><p>Join GetMetricDataV2 Dimensions.AGENT to DescribeUser.User.Id within the same instance. Display IdentityInfo.FirstName / LastName with Username fallback; never join on a name. GetCurrentUserData.UserDataList[].User.Id joins the same identity key. Status.StatusName, Status.StatusStartTimestamp and AvailableSlotsByChannel.VOICE / ActiveSlotsByChannel.VOICE describe a separately timestamped snapshot, not historical performance. Preserve missing values; exclude Contacts and customer details from public payloads.</p></details>':''}</section></div></main>`;
  function render(){
    const shown=metrics.filter(m=>(group==='All metrics'||m.group===group)&&JSON.stringify(m).toLowerCase().includes(search.toLowerCase()));
    dictionary.querySelector('[data-public-count]').textContent=shown.length+' mappings';
    dictionary.querySelectorAll('[data-public-group]').forEach(b=>{const active=b.dataset.publicGroup===group;b.classList.toggle('active',active);b.setAttribute('aria-pressed',String(active));});
    dictionary.querySelector('[data-public-definitions]').innerHTML=shown.map(m=>{
      const spec={Name:m.field};
      if(m.methods)spec.MetricFilters=[{MetricFilterKey:'INITIATION_METHOD',MetricFilterValues:m.methods}];
      if(m.filters)spec.MetricFilters=Object.entries(m.filters).map(([k,v])=>({MetricFilterKey:k,MetricFilterValues:v}));
      if(m.threshold)spec.Threshold=[{Comparison:'LT',ThresholdValue:m.threshold}];
      const filters=m.scope==='profile'?[{FilterKey:'ROUTING_PROFILE',FilterValues:['${ROUTING_PROFILE_ID}']}]:m.scope==='campaign'?[{FilterKey:'CAMPAIGN',FilterValues:['${CAMPAIGN_ARN}']}]:[{FilterKey:'QUEUE',FilterValues:['${STANDARD_QUEUE_ID}']},{FilterKey:'CHANNEL',FilterValues:['VOICE']}];
      const request=m.field?{ResourceArn:'arn:aws:connect:${AWS_REGION}:${AWS_ACCOUNT_ID}:instance/${CONNECT_INSTANCE_ID}',StartTime:'${START_UTC}',EndTime:'${END_UTC_EXCLUSIVE}',Interval:{TimeZone:'America/New_York',IntervalPeriod:'TOTAL'},Filters:filters,Metrics:[spec],...(domain==='agent'?{Groupings:['AGENT']}:{}),MaxResults:100}:{classification:'DERIVED',inputs:m.inputs,formula:(m.inputs||[]).join(' - '),rule:'Join identical scopes, grains, dimensions and intervals; retain unavailable inputs; never label this as a native returned metric.'};
      return `<details class="io-definition" id="public-${domain}-${m.id}"><summary><div><h3>${esc(m.label)}</h3><small>${esc(m.semantic||m.id)}</small><small>${esc(m.field||'Derived arithmetic')}</small></div><strong style="font-size:14px">Data withheld</strong></summary><div class="io-definition-body"><p>${esc(m.meaning)}</p><dl><dt>Classification</dt><dd>${m.field?'Native Amazon metric mapping':'Derived · disclosed arithmetic'}</dd><dt>Unit / grain</dt><dd>${esc(m.unit)} · ${esc(m.grain||m.time_basis||registry.time_basis||'See native metric definition and scope')}</dd><dt>Scope</dt><dd>${esc(domain==='agent'?m.scope==='profile'?'All configured routing profiles; all channels; no queue filter':'Voice; configured standard queues; all initiation methods':m.scope==='campaign'?'Separate campaign population':registry.queue_scope||registry.scope)}</dd><dt>Calculation</dt><dd>${m.inputs?esc(m.inputs.join(' − '))+'; equal request scope required':m.id==='occupancy'?'Native observed ratio × 100 is a derived display conversion; validate unit contract before publishing.':'Preserve the native returned value. Do not average averages or substitute record counts.'}</dd><dt>Amazon documentation</dt><dd><a href="https://docs.aws.amazon.com/connect/latest/APIReference/API_GetMetricDataV2.html" target="_blank" rel="noopener">GetMetricDataV2</a> · <a href="https://docs.aws.amazon.com/connect/latest/adminguide/metrics-definitions.html" target="_blank" rel="noopener">Metric definitions</a></dd></dl><details class="io-technical"><summary>Amazon endpoint, fields and parameterized request</summary><pre>${esc(JSON.stringify(request,null,2))}</pre><p>Placeholders are configuration parameters, not working identifiers or credentials. Follow all pagination tokens. Ungrouped native TOTAL is the team authority; AGENT and DAY are independent reporting grains.</p></details><details class="io-technical"><summary>Snowflake and semantic mapping</summary><p>Semantic metric: <code>${esc(m.semantic||m.id)}</code>. Retain metric identifier, filter specification, UTC interval bounds, timezone, dimensions and native unit. Preserve null, zero and errors separately.</p><pre>${esc(JSON.stringify(registry.warehouse,null,2))}</pre></details></div></details>`;
    }).join('')||'<p>No matching metric definitions.</p>';
  }
  render();
  dictionary.querySelector('[data-public-search]').addEventListener('input',e=>{search=e.target.value;render();});
  dictionary.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.hasAttribute('data-public-dashboard'))switchView(views[0]);if(b.dataset.publicGroup){group=b.dataset.publicGroup;render();}});
  dashboard.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.hasAttribute('data-public-dictionary'))switchView(views[1]);if(b.dataset.publicTopic){group=b.dataset.publicTopic;search='';dictionary.querySelector('[data-public-search]').value='';render();switchView(views[1]);}});
};
